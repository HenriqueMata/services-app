import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { justifyContent: "center" },

  cardContainer: {
    width: 70,
    height: 70,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 40,
  },

  txtLabel: { marginTop: 14, textAlign: "center" },
});

export default styles;
